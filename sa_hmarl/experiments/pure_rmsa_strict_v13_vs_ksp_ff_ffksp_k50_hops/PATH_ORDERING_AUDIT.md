# Path Ordering Audit

All three methods receive the same K=50 paths sorted by hop count, then km, then the canonical deterministic path tie-break. Blocks are `start_asc`; flat encoding is `p*(4*10)+m*10+b`.
